experiment_id: "cost_sensitivity_experiment_v1"
random_seed: 42

data:
  universe: "sp100_20140101"
  start_date: "2014-01-01"
  end_date: "2019-12-31"
  data_source: "yfinance"
  raw_dir: "data/raw"
  processed_dir: "data/processed"
  universe_dir: "data/universe"

signal:
  name: "momentum_12_1"
  params:
    lookback: 252
    skip: 21
  forward_horizon: 20
  hypothesis_type: "confirmatory"
  ranking_method: "percentile"

costs:
  commission_bps: 5.0
  spread_bps: 5.0
  slippage_bps: 5.0
  min_commission: 0.0

portfolio:
  method: "vol_scaled"
  gross_exposure: 1.0
  max_position_weight: 0.10
  max_net_exposure: 0.20

backtest:
  initial_cash: 1000000.0
  execution_price: "next_adj_close"

walk_forward:
  min_train_years: 3
  val_years: 1
  test_years: 1
  step_years: 1
  method: "expanding"
  final_holdout: false

output_dir: "results/experiments/cost_sensitivity_v1"
